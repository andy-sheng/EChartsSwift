diff --git a/Package.swift b/Package.swift
index 755ba54..b7d3ab5 100644
--- a/Package.swift
+++ b/Package.swift
@@ -37,7 +37,10 @@ let package = Package(
             dependencies: ["RasterizerCpp"],
             path: "Sources/RasterizerObjC",
             resources: [
-                .copy("../../Sources/RasterizerCpp/include/Shaders.metal")
+                .copy("../../Sources/RasterizerCpp/include/Shaders.metal"),
+                // LOCAL MODIFICATION (see README-VENDORED.md): also bundle the shared
+                // constants header so the runtime shader-compile fallback can inline it.
+                .copy("../../Sources/RasterizerCpp/include/Rasterizer.h")
             ],
             cxxSettings: [
                 .headerSearchPath("private"),
diff --git a/Sources/RasterizerCpp/include/Shaders.metal b/Sources/RasterizerCpp/include/Shaders.metal
index dcc3a66..afcdde3 100644
--- a/Sources/RasterizerCpp/include/Shaders.metal
+++ b/Sources/RasterizerCpp/include/Shaders.metal
@@ -753,3 +753,35 @@ fragment float4 instances_fragment_main(InstancesVertex vert [[stage_in]],
     float x = vert.tex.x, y = vert.tex.y, z = vert.tex.z;
     return alpha * vert.alpha * clip * colorTexture.sample(cs, float2(z == 0.0 ? x : sqrt(x * x + z * z), y));
 }
+
+#pragma mark - Feedback (LOCAL MODIFICATION — see README-VENDORED.md; NOT upstream)
+// Motion-blur feedback pass: before the scene renders, composite the PREVIOUS frame
+// (retained in a persistent texture) over the clear color at `alpha` — the Metal
+// equivalent of zrender canvas/Layer.ts's back-buffer drawImage(domBack) trick.
+
+struct FeedbackVertex
+{
+    float4 position [[position]];
+    float2 uv;
+};
+
+vertex FeedbackVertex feedback_vertex_main(uint vid [[vertex_id]])
+{
+    // Fullscreen triangle: (0,0) (2,0) (0,2) -> NDC (-1,-1) (3,-1) (-1,3).
+    float2 pos = float2((vid << 1) & 2, vid & 2);
+    FeedbackVertex vert;
+    vert.position = float4(pos * 2.0 - 1.0, 0.0, 1.0);
+    // Texture row 0 is the frame's top; NDC -1 is the bottom -> flip v.
+    vert.uv = float2(pos.x, 1.0 - pos.y);
+    return vert;
+}
+
+fragment float4 feedback_fragment_main(FeedbackVertex vert [[stage_in]],
+                                       texture2d<float> prev [[texture(0)]],
+                                       constant float *alpha [[buffer(0)]])
+{
+    // Premultiplied content scaled by alpha; the pipeline blends One/OneMinusSourceAlpha,
+    // so the result over the clear color is prev*alpha + clear*(1-alpha) — exactly the
+    // canvas globalAlpha composite.
+    return prev.sample(cs, vert.uv) * *alpha;
+}
diff --git a/Sources/RasterizerObjC/RARenderHost.mm b/Sources/RasterizerObjC/RARenderHost.mm
new file mode 100644
index 0000000..5e36642
--- /dev/null
+++ b/Sources/RasterizerObjC/RARenderHost.mm
@@ -0,0 +1,127 @@
+//
+//  RARenderHost.mm — LOCAL ADDITION to the vendored mindbrix/Rasterizer package
+//  (see README-VENDORED.md; NOT part of upstream). Push-model host over the private
+//  RasterizerLayer; mirrors RasterizerView's layer setup + writeBuffer plumbing minus
+//  the display-link pull loop.
+//
+//  Underlying Rasterizer engine © Nigel Timothy Barber (personal-use zlib license).
+//
+
+#import "RARenderHost.h"
+#import <Metal/Metal.h>
+#import "RasterizerLayer.h"
+#import "RasterizerAPI+Internal.h"
+#import "RasterizerRenderer.hpp"
+
+@interface RARenderHost () <LayerDelegate> {
+    // Direct ivar, NEVER a property: an ObjC property getter returns this C++ value BY
+    // COPY, and RasterizerRenderer's contexts hold refcounted Memory/Row pages whose
+    // copies release the shared pages on destruction — every renderList call through a
+    // getter-copy leaves the real renderer's allocators dangling (SIGSEGV in
+    // Allocator::refill on the next frame). Upstream RasterizerView uses `_renderer` too.
+    RasterizerRenderer _renderer;
+}
+@property(nonatomic, strong, nullable) RASceneList *list;
+@property(nonatomic) double listWidth;
+@property(nonatomic) double listHeight;
+@end
+
+@implementation RARenderHost
+
+- (instancetype)initWithScale:(CGFloat)scale {
+    self = [super init];
+    if (!self)
+        return nil;
+    // Mirrors RasterizerView.setUseCG's Metal branch: RasterizerLayer + deviceRGB colorspace,
+    // implicit animations disabled so per-frame contents swaps don't fade.
+    RasterizerLayer *layer = [RasterizerLayer layer];
+    layer.layerDelegate = self;
+    CGColorSpaceRef rgb = CGColorSpaceCreateDeviceRGB();
+    layer.colorspace = rgb;
+    CGColorSpaceRelease(rgb);
+    layer.contentsScale = scale;
+    layer.opaque = YES;
+    layer.needsDisplayOnBoundsChange = YES;
+    layer.actions = @{ @"onOrderIn": [NSNull null], @"onOrderOut": [NSNull null],
+                       @"sublayers": [NSNull null], @"contents": [NSNull null],
+                       @"backgroundColor": [NSNull null], @"bounds": [NSNull null] };
+    _layer = layer;
+    return self;
+}
+
+- (void)presentList:(RASceneList *)list width:(double)width height:(double)height {
+    self.list = list;
+    self.listWidth = width;
+    self.listHeight = height;
+    [self.layer setNeedsDisplay];
+}
+
+#pragma mark - LayerDelegate (called from RasterizerLayer's display pass)
+
+- (void)writeBuffer:(Ra::Buffer *)buffer forLayer:(CALayer *)layer {
+    if (!self.list)
+        return;   // nothing published yet — a fresh Ra::Buffer renders as an empty clear
+    _renderer.renderList(self.list.list, self.layer.contentsScale,
+                         self.listWidth, self.listHeight, buffer);
+}
+
+#pragma mark - Motion-blur feedback + headless Metal access
+
+- (void)setMotionBlurAlpha:(double)alpha {
+    RasterizerLayer *layer = (RasterizerLayer *)self.layer;
+    if (layer.feedbackAlpha != alpha) {
+        layer.feedbackAlpha = alpha;
+        [layer setNeedsDisplay];
+    }
+}
+
+- (void)displayNow {
+    [(RasterizerLayer *)self.layer displayAndWait];
+}
+
+- (CGImageRef)copyFeedbackFrame {
+    RasterizerLayer *layer = (RasterizerLayer *)self.layer;
+    id<MTLTexture> tex = layer.feedbackTexture;
+    if (!tex)
+        return nil;
+    [layer.lastCommandBuffer waitUntilCompleted];   // ensure the GPU blit landed
+    size_t w = tex.width, h = tex.height, bytesPerRow = w * 4;
+    NSMutableData *data = [NSMutableData dataWithLength:h * bytesPerRow];
+    [tex getBytes:data.mutableBytes bytesPerRow:bytesPerRow fromRegion:MTLRegionMake2D(0, 0, w, h) mipmapLevel:0];
+    CGDataProviderRef provider = CGDataProviderCreateWithCFData((__bridge CFDataRef)data);
+    CGColorSpaceRef rgb = CGColorSpaceCreateDeviceRGB();
+    // Drawable format is BGRA8 premultiplied.
+    CGImageRef image = CGImageCreate(w, h, 8, 32, bytesPerRow, rgb,
+                                     kCGImageAlphaPremultipliedFirst | kCGBitmapByteOrder32Little,
+                                     provider, NULL, false, kCGRenderingIntentDefault);
+    CGColorSpaceRelease(rgb);
+    CGDataProviderRelease(provider);
+    return image;
+}
+
+#pragma mark - Headless CPU reference (RasterizerCG)
+
++ (void)renderList:(RASceneList *)list
+             scale:(CGFloat)scale
+             width:(double)width
+            height:(double)height
+       intoContext:(CGContextRef)ctx {
+    RasterizerCG::renderListToBitmap(list.list, scale, width, height, ctx);
+}
+
++ (double)benchList:(RASceneList *)list
+              scale:(CGFloat)scale
+              width:(double)width
+             height:(double)height
+             frames:(int)frames {
+    static RasterizerRenderer renderer;
+    static Ra::Buffer buffer;
+    // Warm-up frame (first frame pays one-time allocations).
+    renderer.renderList(list.list, scale, width, height, & buffer);
+    CFAbsoluteTime t0 = CFAbsoluteTimeGetCurrent();
+    for (int i = 0; i < frames; i++)
+        renderer.renderList(list.list, scale, width, height, & buffer);
+    return (CFAbsoluteTimeGetCurrent() - t0) * 1000.0 / MAX(1, frames);
+}
+
+@end
diff --git a/Sources/RasterizerObjC/RasterizerLayer.mm b/Sources/RasterizerObjC/RasterizerLayer.mm
index 7086a63..e71b45b 100644
--- a/Sources/RasterizerObjC/RasterizerLayer.mm
+++ b/Sources/RasterizerObjC/RasterizerLayer.mm
@@ -107,6 +107,13 @@ struct TextureCache : MetalCache<id <MTLTexture>, const Ra::Paint &> {
 @property (nonatomic) id <MTLTexture> depthTexture;
 @property (nonatomic) id <MTLTexture> accumulationTexture;
 
+// LOCAL MODIFICATION (see README-VENDORED.md) — motion-blur feedback pass state.
+@property (nonatomic) id <MTLRenderPipelineState> feedbackPipelineState;
+@property (nonatomic) id <MTLDepthStencilState> feedbackDepthState;
+@property (nonatomic, readwrite) id <MTLTexture> feedbackTexture;
+@property (nonatomic) BOOL feedbackValid;
+@property (nonatomic, readwrite) id <MTLCommandBuffer> lastCommandBuffer;
+
 @end
 
 
@@ -125,6 +132,28 @@ struct TextureCache : MetalCache<id <MTLTexture>, const Ra::Paint &> {
     
 #if SWIFT_PACKAGE
     self.defaultLibrary = [self.device newDefaultLibraryWithBundle:SWIFTPM_MODULE_BUNDLE error:nil];
+    // LOCAL MODIFICATION (see README-VENDORED.md): the `swift build` CLI copies the
+    // Shaders.metal resource verbatim without compiling a default.metallib (Xcode's SPM
+    // integration does compile it), so newDefaultLibraryWithBundle comes back nil there.
+    // Fall back to runtime-compiling the copied source.
+    if (!self.defaultLibrary) {
+        NSString *path = [SWIFTPM_MODULE_BUNDLE pathForResource:@"Shaders" ofType:@"metal"];
+        NSString *src = path ? [NSString stringWithContentsOfFile:path encoding:NSUTF8StringEncoding error:nil] : nil;
+        if (src) {
+            // The shader source #imports the shared constants header, which the runtime
+            // compiler cannot resolve — inline the bundled copy textually.
+            NSString *hdrPath = [SWIFTPM_MODULE_BUNDLE pathForResource:@"Rasterizer" ofType:@"h"];
+            NSString *hdr = hdrPath ? [NSString stringWithContentsOfFile:hdrPath encoding:NSUTF8StringEncoding error:nil] : nil;
+            if (hdr)
+                src = [src stringByReplacingOccurrencesOfString:@"#import \"Rasterizer.h\"" withString:hdr];
+            NSError *error = nil;
+            self.defaultLibrary = [self.device newLibraryWithSource:src options:nil error:&error];
+            if (!self.defaultLibrary)
+                NSLog(@"RasterizerLayer: runtime shader compile failed: %@", error);
+        } else {
+            NSLog(@"RasterizerLayer: Shaders.metal not found in %@", SWIFTPM_MODULE_BUNDLE);
+        }
+    }
 #else
     self.defaultLibrary = [self.device newDefaultLibrary];
 #endif
@@ -211,18 +240,57 @@ struct TextureCache : MetalCache<id <MTLTexture>, const Ra::Paint &> {
     descriptor.fragmentFunction = [self.defaultLibrary newFunctionWithName:@"quad_molecules_fragment_main"];
     descriptor.label = @"quad molecules";
     self.quadMoleculesPipelineState = [self.device newRenderPipelineStateWithDescriptor:descriptor error:nil];
-    
+
+    // LOCAL MODIFICATION (see README-VENDORED.md) — motion-blur feedback pass: fullscreen
+    // triangle compositing the previous frame at feedbackAlpha, drawn in the drawable pass
+    // (depth/stencil attached) before any scene geometry, without touching depth.
+    MTLRenderPipelineDescriptor *feedbackDescriptor = [MTLRenderPipelineDescriptor new];
+    feedbackDescriptor.colorAttachments[0].pixelFormat = self.pixelFormat;
+    feedbackDescriptor.depthAttachmentPixelFormat = MTLPixelFormatDepth32Float_Stencil8;
+    feedbackDescriptor.stencilAttachmentPixelFormat = MTLPixelFormatDepth32Float_Stencil8;
+    feedbackDescriptor.colorAttachments[0].blendingEnabled = YES;
+    feedbackDescriptor.colorAttachments[0].rgbBlendOperation = MTLBlendOperationAdd;
+    feedbackDescriptor.colorAttachments[0].alphaBlendOperation = MTLBlendOperationAdd;
+    feedbackDescriptor.colorAttachments[0].sourceRGBBlendFactor = MTLBlendFactorOne;
+    feedbackDescriptor.colorAttachments[0].sourceAlphaBlendFactor = MTLBlendFactorOne;
+    feedbackDescriptor.colorAttachments[0].destinationRGBBlendFactor = MTLBlendFactorOneMinusSourceAlpha;
+    feedbackDescriptor.colorAttachments[0].destinationAlphaBlendFactor = MTLBlendFactorOneMinusSourceAlpha;
+    feedbackDescriptor.vertexFunction = [self.defaultLibrary newFunctionWithName:@"feedback_vertex_main"];
+    feedbackDescriptor.fragmentFunction = [self.defaultLibrary newFunctionWithName:@"feedback_fragment_main"];
+    feedbackDescriptor.label = @"feedback";
+    self.feedbackPipelineState = [self.device newRenderPipelineStateWithDescriptor:feedbackDescriptor error:nil];
+
+    MTLDepthStencilDescriptor *feedbackDepthDescriptor = [MTLDepthStencilDescriptor new];
+    feedbackDepthDescriptor.depthWriteEnabled = NO;
+    feedbackDepthDescriptor.depthCompareFunction = MTLCompareFunctionAlways;
+    self.feedbackDepthState = [self.device newDepthStencilStateWithDescriptor:feedbackDepthDescriptor];
+
     return self;
 }
 
 - (void)display {
     @autoreleasepool {
+        // LOCAL MODIFICATION: the feedback blit reads the drawable, which framebufferOnly forbids.
+        self.framebufferOnly = !(self.feedbackAlpha > 0);
         self.drawableSize = CGSizeMake(ceil(self.bounds.size.width * self.contentsScale), ceil(self.bounds.size.height * self.contentsScale));
         if (dispatch_semaphore_wait(_inflight_semaphore, DISPATCH_TIME_NOW) == 0)
             [self draw];
     }
 }
 
+// LOCAL MODIFICATION (see README-VENDORED.md) — synchronous render for headless testing:
+// display drops frames when both in-flight slots are busy (fine for live pacing); driving
+// back-to-back frames from a loop needs every frame to land.
+- (void)displayAndWait {
+    @autoreleasepool {
+        self.framebufferOnly = !(self.feedbackAlpha > 0);
+        self.drawableSize = CGSizeMake(ceil(self.bounds.size.width * self.contentsScale), ceil(self.bounds.size.height * self.contentsScale));
+        dispatch_semaphore_wait(_inflight_semaphore, DISPATCH_TIME_FOREVER);
+        [self draw];
+        [self.lastCommandBuffer waitUntilCompleted];
+    }
+}
+
 - (void)draw {
     BOOL odd = ++_tick & 1;
     Ra::Buffer *buffer = odd ? & _buffer1 : & _buffer0;
@@ -298,7 +366,33 @@ struct TextureCache : MetalCache<id <MTLTexture>, const Ra::Paint &> {
     clipDescriptor.stencilAttachment.clearStencil = 0;
     
     id <MTLRenderCommandEncoder> commandEncoder = [commandBuffer renderCommandEncoderWithDescriptor:drawableDescriptor];
-    
+
+    // LOCAL MODIFICATION (see README-VENDORED.md) — motion-blur feedback: composite the
+    // previous frame at feedbackAlpha over the fresh clear, before any scene geometry.
+    if (self.feedbackAlpha > 0) {
+        if (self.drawableSize.width != self.feedbackTexture.width || self.drawableSize.height != self.feedbackTexture.height) {
+            MTLTextureDescriptor *fdesc = [MTLTextureDescriptor texture2DDescriptorWithPixelFormat:self.pixelFormat
+                                             width:self.drawableSize.width
+                                            height:self.drawableSize.height
+                                         mipmapped:NO];
+            fdesc.storageMode = MTLStorageModeShared;   // CPU-readable for headless verification
+            fdesc.usage = MTLTextureUsageShaderRead;
+            self.feedbackTexture = [self.device newTextureWithDescriptor:fdesc];
+            [self.feedbackTexture setLabel:@"feedbackTexture"];
+            self.feedbackValid = NO;                    // fresh texture holds garbage
+        }
+        if (self.feedbackValid) {
+            float feedbackAlpha = self.feedbackAlpha;
+            [commandEncoder setRenderPipelineState:_feedbackPipelineState];
+            [commandEncoder setDepthStencilState:_feedbackDepthState];
+            [commandEncoder setFragmentTexture:self.feedbackTexture atIndex:0];
+            [commandEncoder setFragmentBytes:& feedbackAlpha length:sizeof(feedbackAlpha) atIndex:0];
+            [commandEncoder drawPrimitives:MTLPrimitiveTypeTriangle vertexStart:0 vertexCount:3];
+        }
+    } else {
+        self.feedbackValid = NO;
+    }
+
     drawableDescriptor.colorAttachments[0].loadAction = MTLLoadActionLoad;
     drawableDescriptor.depthAttachment.loadAction = MTLLoadActionLoad;
     
@@ -438,11 +532,21 @@ struct TextureCache : MetalCache<id <MTLTexture>, const Ra::Paint &> {
         }
     }
     [commandEncoder endEncoding];
+
+    // LOCAL MODIFICATION — retain this frame for the next frame's feedback composite.
+    if (self.feedbackAlpha > 0 && self.feedbackTexture && drawable.texture) {
+        id <MTLBlitCommandEncoder> blit = [commandBuffer blitCommandEncoder];
+        [blit copyFromTexture:drawable.texture toTexture:self.feedbackTexture];
+        [blit endEncoding];
+        self.feedbackValid = YES;
+    }
+
     __block dispatch_semaphore_t block_sema = _inflight_semaphore;
     [commandBuffer addCompletedHandler:^(id <MTLCommandBuffer> buffer) {
         dispatch_semaphore_signal(block_sema);
     }];
     [commandBuffer presentDrawable:drawable];
     [commandBuffer commit];
+    self.lastCommandBuffer = commandBuffer;
 }
 @end
diff --git a/Sources/RasterizerObjC/include/RARenderHost.h b/Sources/RasterizerObjC/include/RARenderHost.h
new file mode 100644
index 0000000..a685ef0
--- /dev/null
+++ b/Sources/RasterizerObjC/include/RARenderHost.h
@@ -0,0 +1,65 @@
+//
+//  RARenderHost.h — LOCAL ADDITION to the vendored mindbrix/Rasterizer package
+//  (see README-VENDORED.md; NOT part of upstream).
+//
+//  A push-model render host: wraps the (private) RasterizerLayer CAMetalLayer so an
+//  EXTERNAL frame clock can drive rendering. The upstream RasterizerView owns its own
+//  CVDisplayLink/CADisplayLink and PULLS a scene list from its delegate every frame;
+//  ZRenderKit already has an AnimationLoop that knows exactly when the scene changed,
+//  so the painter PUSHES the freshly built RASceneList here instead and the layer
+//  redraws once per push (no second clock, no redundant frames).
+//
+//  Underlying Rasterizer engine © Nigel Timothy Barber (personal-use zlib license).
+//
+
+#import <Foundation/Foundation.h>
+#import <QuartzCore/QuartzCore.h>
+#import <CoreGraphics/CoreGraphics.h>
+#import "RasterizerAPI.h"
+
+NS_ASSUME_NONNULL_BEGIN
+
+@interface RARenderHost : NSObject
+
+/// The Metal-backed layer the scene renders into. Add it to a view's layer tree.
+@property(nonatomic, readonly) CALayer *layer;
+
+- (instancetype)initWithScale:(CGFloat)scale;
+
+/// Publish a new scene list (logical width/height in points) and schedule a redraw.
+/// The Metal pipeline renders it on the layer's next display pass.
+- (void)presentList:(RASceneList *)list width:(double)width height:(double)height;
+
+/// Headless CPU reference render of `list` via RasterizerCG (the same interpreter the
+/// engine's own useCG debug mode uses) into `ctx`. Verifies scene translation without
+/// a display / drawable.
++ (void)renderList:(RASceneList *)list
+             scale:(CGFloat)scale
+             width:(double)width
+            height:(double)height
+       intoContext:(CGContextRef)ctx;
+
+/// Motion-blur feedback (zr.configLayer): when alpha > 0, every frame starts by
+/// compositing the previous frame at this alpha over the clear color — the Metal analog
+/// of zrender canvas/Layer.ts's back-buffer drawImage(domBack, globalAlpha) trick.
+- (void)setMotionBlurAlpha:(double)alpha;
+
+/// Force a synchronous Metal render pass of the last presented list (headless testing —
+/// no CoreAnimation commit cycle required).
+- (void)displayNow;
+
+/// CPU readback of the retained feedback frame (valid after a displayNow/display with
+/// motion blur enabled) — a real screenshot of the Metal pipeline's output.
+- (nullable CGImageRef)copyFeedbackFrame CF_RETURNS_RETAINED;
+
+/// Benchmark the engine's CPU stage (Context::drawList fan-out into an Ra::Buffer — the
+/// same work writeBuffer does per frame, minus the GPU encode). Returns avg ms/frame.
++ (double)benchList:(RASceneList *)list
+              scale:(CGFloat)scale
+              width:(double)width
+             height:(double)height
+             frames:(int)frames;
+
+@end
+
+NS_ASSUME_NONNULL_END
diff --git a/Sources/RasterizerObjC/private/RasterizerLayer.h b/Sources/RasterizerObjC/private/RasterizerLayer.h
index e3ea3fb..562efe9 100644
--- a/Sources/RasterizerObjC/private/RasterizerLayer.h
+++ b/Sources/RasterizerObjC/private/RasterizerLayer.h
@@ -29,4 +29,17 @@
 
 @interface RasterizerLayer : CAMetalLayer
 @property(weak) id <LayerDelegate> layerDelegate;
+
+// LOCAL MODIFICATION (see README-VENDORED.md; NOT upstream) — motion-blur feedback:
+// when > 0, each frame starts by compositing the PREVIOUS frame at this alpha over the
+// clear color (the Metal analog of zrender canvas/Layer.ts's back-buffer drawImage),
+// and ends by blitting the drawable into the persistent feedback texture.
+@property(nonatomic) double feedbackAlpha;
+// The retained previous frame (BGRA8, shared storage — CPU-readable) and the command
+// buffer of the most recent draw (for CPU readback synchronization).
+@property(nonatomic, readonly, nullable) id<MTLTexture> feedbackTexture;
+@property(nonatomic, readonly, nullable) id<MTLCommandBuffer> lastCommandBuffer;
+// Synchronous render (headless testing): unlike display, WAITS for an in-flight slot
+// instead of dropping the frame, and blocks until the GPU finishes.
+- (void)displayAndWait;
 @end
