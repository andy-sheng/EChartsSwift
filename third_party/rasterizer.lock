# Pinned source for the vendored mindbrix/Rasterizer engine (the experimental
# Metal painter's backend — see Sources/RasterizerPainter).
#
# third_party/Rasterizer/ is NOT committed (gitignored, like upstream/*): it is
# reconstructed as <repo>/Package + LICENSE.txt at the EXACT commit below, with
# third_party/rasterizer-local.patch applied on top (our local modifications —
# RARenderHost bridge, motion-blur feedback pass, shader-compile fallback; each
# marked in-source per the license's altered-source clause).
#
# Reconstruct / update the checkout:
#     scripts/sync-rasterizer.sh
# Verify it matches this lock + patch:
#     scripts/sync-rasterizer.sh --check
#
# License: personal-use zlib (commercial use needs a licence from the author,
# nigel@mindbrix.co.uk). See third_party/README-Rasterizer.md.
#
# Format (whitespace-separated): <name> <git-url> <commit-sha>
rasterizer https://github.com/mindbrix/Rasterizer.git 7058139eeab11b4054c31eca97aa6d1f2d1cb90c
